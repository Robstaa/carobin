<% provide :head_tags do %>
    <script>
        document.addEventListener("turbolinks:load", () => {
            initMap({
                centerLat: <%=@center.latitude%>, 
                centerLng: <%=@center.longitude%>, 
                markers:   <%= @markers %>
            });
        });
    </script>
<% end %>

<%= link_to 'Neuer Ort', new_place_path %>
<%= render 'layouts/map' %>